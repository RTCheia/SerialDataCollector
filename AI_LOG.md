# AI 编程与需求跟踪记录 (AI_LOG.md)

本文档用于记录多串口数据采集与自动解析上位机项目的需求演进、技术选型、设计方案以及每一步的操作历史，防止因长上下文截断导致信息丢失。

---

## 一、 项目背景与原始流程

### 1.1 原始手工流程
1. **串口接收**：通过 `UartAssist.exe`（串口调试助手）连接设备，勾选 16 进制显示，配置“接收转向文件”，将原始十六进制字节流输出到 `d.txt`。
2. **数据解析转换**：运行 `transimuuwb.exe`，该程序根据配置文件 `fr.txt` 的解析规则，将 `d.txt` 翻译解码成物理量数据表 `data.txt`。

### 1.2 存在痛点
- 手动多步骤中转，效率低；
- 后续需要**同时连接 3 个串口**，多开 UartAssist 手动重定向容易混乱且繁琐；
- 产生体积庞大的 `d.txt` 中间文件，读写开销大。

---

## 二、 核心需求整理

1. **多串口支持**：
   - 支持同时连接 1~3 个串口（可灵活勾选启用）。
   - 每个通道可独立配置串口号（COM口）、波特率（默认 230400，8位数据位, 无校验, 1位停止位）、保存文件名。
   - 提供“刷新串口”功能，方便热插拔。

2. **界面与交互形态（双版本演进）**：
   - **V1.0 (极简版已交付)**：配置串口、开始/结束采集，直接输出 `data_ch*.txt`，已打包 `SerialDataCollector.exe`。
   - **V2.0 (实时波形与全屏预览版，开发中)**：
     - **支持全屏显示**（F11 或最大化窗口切换）。
     - **实时数据曲线波形预览**（基于 Matplotlib Canvas 集成）。
     - **3 个独立视图框（参考历史 MATLAB 综合对比图布局）**：
       - **框 1：IMU 6轴惯导**（分上、下两子图，上为角速度 `wx, wy, wz` deg/s，下为加速度 `ax, ay, az` m/s²，对应列 3~8）。
       - **框 2：大量程加速度计 (3轴)**（`X, Y, Z` m/s²，对应列 9~11）。
       - **框 3：UWB 空间位置 (3轴)**（`pos_x, pos_y, pos_z` m，对应列 16~18）。
     - **通道切换控制**：在预览区提供「通道1 (CH1)」、「通道2 (CH2)」、「通道3 (CH3)」快速切换按钮，方便在 3 个串口之间自如切换监控波形。

3. **内存级直接转换（无需中间文件）**：
   - 串口二进制字节流在内存滑窗中自动通过 `0xEB 0x90` 寻包，依据 `fr.txt`（23列规则，61字节）直接解析，实时落盘与图表绘制双轨并行。

4. **独立 Windows 交付**：
   - V1.0 保留在 `app.py` / `SerialDataCollector.exe`。
   - V2.0 命名为 `app_v2.py`，并将打包生成 `SerialDataCollector_V2.exe`。

---

## 三、 执行历史与变更日志

| 时间 | 操作内容 | 关键产物 / 状态 |
| :--- | :--- | :--- |
| 2026-09-12 | 分析 `fr.txt`、`d.txt` 和 `data.txt` 数据结构，验证帧长度（61字节）和数据解码精度算法 | 算法验证完成，与原程序 100% 对齐 |
| 2026-09-12 | 编写极简 GUI 脚本，支持 3 串口多线程并行采集与帧对齐直接转换 | 生成 `app.py` |
| 2026-09-12 | 采用方案 A（PyInstaller）进行单文件打包 | 生成独立运行程序 `SerialDataCollector.exe`（11.4MB） |
| 2026-09-12 | 创建本规范记录文档，并编制完整执行方案供审阅 | 生成 `AI_LOG.md`，提交 Implementation Plan 方案 |
| 2026-09-12 | 执行方案通过，完成产物一致性验证并正式就绪 | 所有关键组件（exe, app.py, fr.txt, AI_LOG.md）完备可用 |
| 2026-09-12 | 根据用户串口配置截图更新默认参数：波特率默认调整为 230400（8位数据位, 无校验, 1位停止位），重新打包更新 exe | 更新 `app.py`，重新生成 `SerialDataCollector.exe` |
| 2026-09-12 | 开发 V2.0 升级版：实现全屏支持（F11/Esc）、3框横置实时波形（IMU6轴、大量程加速度3轴、UWB位置3轴）、3通道快速切换按钮；完成单文件封包 | 生成源码 `app_v2.py` 与独立程序 `SerialDataCollector_V2.exe` |
| 2026-09-12 | 增加串口模拟仿真功能：提供独立发包脚本 `simulate_serial.py`，并在 V2.0 上位机中内嵌“【仿真】d.txt回放”虚拟端口，支持免外部工具无硬件自测 | 新增 `simulate_serial.py`，更新并重新打包 `SerialDataCollector_V2.exe` |
| 2026-09-12 | 将 `fr.txt` 规则作为内置缺省配置硬编码（写死）入 V1 (`app.py`) 与 V2 (`app_v2.py`)，脱离外部文件依赖直接双击运行，同时保留有外部文件时的覆盖加载能力；重新打包完成 | 重新编译生成 `SerialDataCollector.exe` 与 `SerialDataCollector_V2.exe` |
| 2026-09-12 | 全新发布 V3.0 (VOFA Modern Flat Edition)：<br>1. 支持指定保存目录，默认 `./data`，收纳 txt 数据与导出的 png 大图；<br>2. 结束采集时后台异步渲染并保存全程完整 3 框高清大图 (`_waveform.png`)；<br>3. 实时预览增加 X 轴采样时间窗加减号 (➕/➖) 交互微调；<br>4. 全局字号放大、采用 VOFA+ 现代工业美学设计；<br>5. 规则内置写死，独立封包单文件 exe。 | 生成源码 `app_v3.py` 与独立程序 `SerialDataCollector_V3.exe` (41.9MB) |
| 2026-09-12 | V3.0 专项优化：4K 高分屏 DPI-Aware 矢量渲染与浅色主题切换 | 更新 `app_v3.py` 并重新打包 `SerialDataCollector_V3.exe` |
| 2026-09-12 | 全新发布 V4.0 (现代极简圆角浅色版)：<br>1. 彻底移除顶部 "VOFA+" 字符，改用纯净专业标题与功能展示；<br>2. 针对意外中断/断电数据保护需求：采用高频内存流刷盘 + 每 0.5s 强制 `os.fsync` 底层扇区同步方案，实现边采边物理落盘（即使任务管理器强杀进程或断电拔线，数据毫秒不漏，无需覆盖写）；<br>3. UI 元素全面采用自绘高清抗锯齿「圆角矩形按钮族」与「圆角矩形卡片容器」；<br>4. 独立命名为 V4，前代版本全量保留。 | 生成源码 `app_v4.py` 与独立程序 `SerialDataCollector_V4.exe` (41.9MB) |
| 2026-09-18 | V4 性能与防丢帧专项升级（10 FPS 渲染锁定 + 零丢包录制）：<br>1. **严格 10 FPS 渲染锁**：将 UI 波形刷新率严格锁定在 10 FPS（每 100ms 刷新一次），并采用数据抽取，彻底消灭渲染占用导致的卡顿；<br>2. **完全解耦采集与显示**：串口接收与解码独立运行在各通道子线程，不受界面渲染耗时影响；<br>3. **批量写盘 + 物理落盘**：移除逐帧 flush 瓶颈，采用批量冲刷（每 50 帧或 0.2s）+ 0.5s `os.fsync` 硬件同步，写入吞吐量提升百倍，彻底杜绝丢包；<br>4. **扩大底层缓冲**：串口缓冲区单次读取扩容至 2048 字节，避免硬件溢出。 | 更新 `app_v4.py` 并重新封包 `SerialDataCollector_V4.exe` |
| 2026-09-18 | 全新发布 V4.1 (高性能低系统I/O版)：<br>1. **彻底拔除硬件级 `os.fsync` 阻塞**：取消每 0.5s 向硬盘物理打穿刷盘的指令，避免 3 个通道并发导致 Windows 底层磁盘 I/O 队列挂起与整机冻结；<br>2. **改用高效操作系统级 PageCache 缓冲落盘**：保留每 50 帧或 0.2s 的 `flush()`，确保即使串口突然掉线或进程被杀，操作系统已接管写入，数据绝对安全；<br>3. 保持 10 FPS 硬锁频与零丢包多线程架构，独立封包为 V4.1。 | 生成源码 `app_v4_1.py` 与独立程序 `SerialDataCollector_V4_1.exe` |
| 2026-09-18 | 编制系统深度剖析与架构交接文档：全面复盘卡顿根因（Matplotlib 纯 CPU 软件渲染、4K 高分屏像素暴增、GIL 线程争抢），并规划基于「PyQt6/PySide6 + PyQtGraph + OpenGL GPU 硬件加速」的方案 2 重构蓝图。 | 产出交接文档 `HANDOVER_ARCHITECTURE.md` |

---

## 四、 下一步计划 / 待办事项 (TODO)
- [x] 完成 V1.0 极简版保留与交付 (`SerialDataCollector.exe`)
- [x] 完成 V2.0 实时波形+全屏版交付 (`SerialDataCollector_V2.exe`)
- [x] 完成 V3.0 VOFA 浅色纯净版交付 (`SerialDataCollector_V3.exe`)
- [x] 完成 V4.0 现代圆角矩形版交付 (`SerialDataCollector_V4.exe`)
- [x] 完成 V4.1 高性能低 I/O 释放整机卡顿版交付 (`SerialDataCollector_V4_1.exe`)
- [x] 编制并交付《现状技术交接与 GPU 重构方案文档》 (`HANDOVER_ARCHITECTURE.md`)
- [ ] 后续启动方案 2 重构：安装 PyQt6/PyQtGraph 并编写 GPU 硬件加速上位机 V5.0
