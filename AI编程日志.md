## 2026-09-18 13:35 - V5 显示窗口扩展至 2000 点及 X 轴帧号/秒数切换

- 修改文件：`acquisition_v5.py`、`ui_v5.py`、`V5使用说明.md`、`README.md`、`README_EN.md`
- 功能说明：扩展共享内存快照容量至 2000 点，UI 窗口调整上限扩展为 50~2000 点；新增 X 轴显示模式切换（显示帧号 vs 显示时间 s），支持手动设定设备采样帧率（默认 200 Hz，范围 1~5000 Hz），波形与时间轴标签实时自适应换算。
- 变更摘要：源码核心自检通过；UI 冒烟测试通过并截取最新截图（经实测 2000 点在 PySide6/PyQtGraph 下渲染仅需 3.3 ms，极其流畅）。

## 2026-09-18 11:25 - V5 记录与预览隔离

- 修改文件：`app_v5.py`、`protocol_v5.py`、`acquisition_v5.py`、`ui_v5.py`、`export_v5.py`、`selftest_v5.py`、`_temp/tools/build_v5.ps1`、`AGENTS.md`
- 功能说明：新增独立 V5，保留所有旧版源码与 EXE，三路串口默认 230440，优先保障记录收尾及预览隔离。
- 变更摘要：每路独立采集进程、接收与写入分线程、有界记录队列、非阻塞共享预览；使用 PySide6/PyQtGraph，预览支持关闭/1/5/10 FPS；停止排空记录队列后关闭文件；按用户补充要求仅保存 TXT，不保存原始字节流；记录计数异常及运行摘要，独立进程导出 PNG；补充源码和 EXE 通用自检及构建脚本。验证结果后续追加。

## 2026-09-18 11:21 - 按短时采集需求取消原始流并验证

- 修改文件：acquisition_v5.py、protocol_v5.py、ui_v5.py、V5使用说明.md
- 功能说明：按用户补充要求只保存 TXT；修正状态提示与空通道预览，保留异常日志及停止收尾。
- 变更摘要：源码核心自检三路各 3000 帧通过；真实 Windows Qt 界面三路仿真启动/绘图/停止通过；三路完整 PNG 导出通过；正在进行冻结 EXE 验证。

## 2026-09-18 11:22 - 增强冻结程序界面自检

- 修改文件：ui_v5.py
- 功能说明：自动覆盖 1/关闭/10/5 FPS 切换、第三路预览、停止与完整波形导出，读取实际文件给出明确通过/失败报告。
- 变更摘要：源码界面自检通过；最终 EXE 正在重建。

## 2026-09-18 11:27 - 打包启动诊断与失败恢复

- 修改文件：app_v5.py、ui_v5.py、requirements-v5.txt
- 功能说明：冻结 EXE 记录核心自检通过，界面依赖导入失败正在定位；新增启动阶段异常日志，避免故障无记录。
- 变更摘要：首次子进程启动失败时恢复操作按钮；固化验证环境依赖版本；进一步检查打包依赖。

## 2026-09-18 11:28 - 修复 Qt 冻结程序 ICU 依赖冲突

- 修改文件：_temp/tools/build_v5.ps1
- 功能说明：PyInstaller 从外部 Poppler 的 PATH 误收集了 icuuc.dll，该库不导出 Qt6Core 所需的无版本后缀接口，导致 QtCore 导入失败。
- 变更摘要：已用 PE 导入/导出表验证缺失符号；构建时限制 PATH 并优先 Windows 系统目录，避免同名第三方 DLL 污染。

## 2026-09-18 11:30 - 验证采集中关闭窗口

- 修改文件：_temp/tools/verify_window_close_v5.py、AGENTS.md
- 功能说明：新增主动关闭窗口的三路仿真收尾测试。
- 变更摘要：测试通过，三路进程均正常退出、计数异常为 0；最终源码核心测试再次通过。

## 2026-09-18 11:32 - 构建配置显式排除冲突 ICU

- 修改文件：_temp/tools/SerialDataCollector_V5.spec、_temp/tools/build_v5.ps1、AGENTS.md
- 功能说明：环境启动钩子仍会注入外部 DLL 路径，单独限制 PATH 无法完全解决。
- 变更摘要：维护专用 spec，打包列表显式排除第三方 icuuc.dll 和 icudt78.dll，Qt 使用 Windows 系统 ICU。

## 2026-09-18 11:34 - 构建脚本 Windows 编码兼容

- 修改文件：_temp/tools/build_v5.ps1
- 功能说明：构建脚本保存为 UTF-8 BOM，兼容 Windows PowerShell 对中文注释的读取。
- 变更摘要：通过专用 spec 直接构建修正版，详细日志为 log/build_v5_final.log。

## 2026-09-18 11:35 - V5 发布验收通过

- 修改文件：SerialDataCollector_V5.exe、log/v5_release_validation.log
- 功能说明：修复 QtCore 的 ICU DLL 打包冲突，最终 EXE 已实际启动验证。
- 变更摘要：冻结 EXE 三路各 3000 帧共 9000 帧逐行核验通过；UI 三路仿真、1/关闭/10/5 FPS 切换、CH3 预览、停止收尾、独立 PNG 导出全部通过，进程正常退出；归档 SHA256 和验证路径。默认 230440，只记录 TXT，不存原始字节流；未进行真实三路硬件验收。
