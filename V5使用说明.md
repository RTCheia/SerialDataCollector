# 多串口数据采集 V5

双击 `SerialDataCollector_V5.exe` 即可运行，不需要安装 Python。旧版程序全部保留。

## 采集

1. 勾选需要记录的 CH1～CH3，为每路选择不同的物理串口。
2. 默认参数为用户指定的 **230440 / 8N1**。波特率可以修改，请与下位机一致；V4 默认值为 230400，二者并非同一个数字。驱动不接受该波特率时，程序会报告通道失败，不会暗中改成其他值。
3. 选择保存目录，点击“开始记录”。等各路显示“记录中”后进行实验。三路独立打开，软件不提供硬件级同步触发。
4. 实验完成点击“停止并保存”，等待显示“已保存”。通常 10 秒以内也可正常使用，没有强制 10 秒自动停止。
5. 关闭窗口时也会请求停止，等待写完记录再关闭。不要使用任务管理器强制结束代替正常停止。

默认只保存解析后的 TXT，**不保存原始字节流或 BIN**。TXT 保留 V4 的 23 列顺序、列宽、8 位小数，第二列为 0～255 帧计数。

## 输出文件

每次采集创建唯一目录，不覆盖旧记录：

```text
data/日期_时间_唯一编号/
  ch1/data.txt
  ch1/summary.json
  ch1/log/acquisition_v5.log
  ch2/...
  ch3/...
```

`summary.json` 包含接收/处理字节数、记录/flush 帧数、计数异常次数、帧同步丢弃字节数、停止边界不足帧字节数及错误。仅启用的通道会生成文件。

“计数异常 0”表示没有检测到相邻计数跳变。计数模 256 回绕，无法仅凭计数排除一次丢失 256 的整数倍帧；协议末尾校验字段原样记录，因未提供校验算法，未执行校验和验证。用户点击停止后不继续无限追收设备数据；记录会排空停止时驱动中已有的数据和应用记录队列，末尾不足一帧的字节数量写入摘要，不写入 TXT。

## 预览

- 使用 PySide6 + PyQtGraph；默认 **5 FPS**，可选 1 FPS、10 FPS 或关闭预览。
- 关闭预览、切换通道、最小化窗口不停止记录。最小化及数据未变化时不更新曲线。
- 默认显示最近 500 点，窗口范围可调为 **50～2000 点**。Y 轴每秒调整一次，可关闭自动范围。
- 支持 **X 轴显示模式切换**：可选“显示帧号”或“显示时间 (s)”，支持手动设置设备采样帧率（默认 200 Hz，范围 1～5000 Hz），自适应换算并在切换时即时重绘。
- 显示抽样仅影响预览，完整 TXT 不做抽样。默认关闭抗锯齿和 OpenGL，以减少负载及显卡兼容性问题；独立采集进程与非阻塞预览快照是防止预览拖住记录的核心。
- F11 全屏，Esc 返回普通窗口。图上“更新 ms”仅表示数据及范围更新调用耗时，不是完整 GPU/屏幕呈现时长。
- “仿真数据”是内置 200 Hz 测试源，不表示真实设备已连接。

## 完整波形导出

停止后点击“导出本次完整波形 PNG”，在独立进程生成各路 `waveform.png`，结果见 `export_result.json`。10 秒内的正常数据通常逐点绘制；超过 10000 帧时使用保留极值的分组包络，TXT 数据不改变。

## 可靠性边界

每路独立采集进程，接收与写入分线程；记录队列满、磁盘写入失败、串口拔线会明确标记失败，并请求其他通道停止，防止误当作完整三路实验。运行时每约 0.2 秒 flush，停止时同步文件并关闭；突然断电或强杀不能保证最后尚未提交的数据完整。

已提供软件自检，真实三路串口及设备对 230440 的支持仍需在实际实验硬件上验收。

## 发布验证（2026-09-18）

最终 EXE 已通过三路共 9000 帧逐行核验、预览锁阻塞隔离、模拟拔线、防覆盖、1/关闭/10/5 FPS 切换、第三路预览、停止收尾和三路 PNG 导出测试。QtCore DLL 依赖冲突已修复并验证。测试使用仿真源，尚未进行真实三路设备验收。结果索引见 log/v5_release_validation.log。
